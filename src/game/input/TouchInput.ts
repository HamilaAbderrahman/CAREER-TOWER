/**
 * Touch state written by Phaser pointer handlers (in Player.ts).
 * Read each frame in Player.update().
 */
export const TouchInput = {
  left:        false,
  right:       false,
  jumpQueued:  false,
}
